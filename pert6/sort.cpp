#include<iostream>
#include<time.h>

using namespace std;

class TSort{
    private:
        template<typename T>
        void merge_split(T *data, size_t L, size_t M, size_t R){
            int n1 = (m - l + 1);
            int n2 = (r - m);
            size_t x = 0;
            size_t y = 0;
            size_t z = 0;
            
            T *L;
            T *R;

        }
}